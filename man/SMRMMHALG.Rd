% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc.R
\name{SMRMMHALG}
\alias{SMRMMHALG}
\title{Metropolis-Hastings sampler with shared weights within classes}
\usage{
SMRMMHALG(
  B = 5,
  J = 20,
  N1vec = 150,
  N2vec = 200,
  N0vec = 50,
  M1vec = 40,
  M2vec = 60,
  mvec = 10,
  X,
  B0,
  V0,
  eta = 0.001,
  tau = 1000,
  mu0 = 0,
  sig0 = 0.001,
  muinit = 0,
  rinit = 0,
  del = 0.1,
  n = length(mvec)
)
}
\arguments{
\item{B}{Integer; number of burn-in iterations.}

\item{J}{Integer; number of sampling iterations retained after burn-in.}

\item{N1vec, N2vec}{Numeric vectors; \code{N1} and \code{N2} for each observation.}

\item{N0vec}{Numeric vector; number of common items \code{N} for each
observation.}

\item{M1vec, M2vec}{Numeric vectors; sample sizes \code{m1} and \code{m2} for each
observation.}

\item{mvec}{Numeric vector; observed numbers of matches.}

\item{X}{Numeric matrix; design matrix linking classes to coefficients.}

\item{B0}{Numeric vector; prior mean of the regression coefficients.}

\item{V0}{Numeric matrix; prior covariance of the regression coefficients.}

\item{eta, tau}{Numeric; shape and rate hyperparameters of the gamma prior
on the precision \code{lambda}.}

\item{mu0, sig0}{Numeric; mean and scale hyperparameters of the normal prior
on \code{mu}.}

\item{muinit, rinit}{Numeric; starting values for \code{mu} and the latent
weights.}

\item{del}{Numeric; random-walk proposal standard deviation(s).}

\item{n}{Integer; number of observations (defaults to \code{length(mvec)}).}
}
\value{
A list with components \code{betv}, \code{lamv}, \code{rm} and \code{rar}, as in
\code{\link[=RMMHALG]{RMMHALG()}}.
}
\description{
Variant of \code{\link[=RMMHALG]{RMMHALG()}} that infers the weight classes from the design
matrix \code{X} and constrains observations mapping to the same linear predictor
to share a common weight.
}
\examples{
set.seed(8391)
X <- cbind(1, c(1, 0, 1))
res <- SMRMMHALG(
  B = 2, J = 5,
  N1vec = c(150, 150, 150), N2vec = c(200, 200, 200),
  N0vec = c(50, 50, 50), M1vec = c(40, 40, 40), M2vec = c(60, 60, 60),
  mvec = c(10, 11, 9),
  X = X, B0 = c(0, 0), V0 = diag(2)
)
str(res)

}
\references{
Puza, B. and Bonfrer, A. (2018). A series of two-urn biased sampling
problems. \emph{Communications in Statistics - Theory and Methods}, 47(1),
80-91. \doi{10.1080/03610926.2017.1300282}
}
