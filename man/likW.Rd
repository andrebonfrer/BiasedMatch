% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{likW}
\alias{likW}
\title{Likelihood and posterior curves for the bias parameter}
\usage{
likW(m, N1, N2, N, m1, m2, wgrid = seq(0.05, 6, by = 0.05))
}
\arguments{
\item{m}{Integer; number of matches for which the probability is required.}

\item{N1}{Integer; number of distinct items in urn 1.}

\item{N2}{Integer; number of distinct items in urn 2.}

\item{N}{Integer; number of items common to both urns.}

\item{m1}{Integer; sample size drawn from urn 1.}

\item{m2}{Integer; sample size drawn from urn 2.}

\item{wgrid}{Numeric vector of positive weight values at which to evaluate
the curves.}
}
\value{
A data frame with columns:
\describe{
\item{w}{The weight grid (as supplied).}
\item{likelihood}{Likelihood values standardised to a maximum of 1.}
\item{posterior}{Posterior-kernel values (likelihood / \code{w}) standardised
to a maximum of 1.}
}
}
\description{
Evaluates, over a grid of weight values, the likelihood of the bias
parameter \code{w} given an observed number of matches \code{m}, together with the
kernel of the posterior density under the prior \code{f(w)} proportional to
\code{1 / w}. Both curves are returned standardised to a maximum of 1, matching
the figures in Puza and Bonfrer (2018).
}
\examples{
# Reproduce the likelihood/posterior curves of Problem 3 (marketing data)
curves <- likW(
  m = 13, N1 = 292, N2 = 350, N = 192, m1 = 90, m2 = 59,
  wgrid = seq(0.05, 8, by = 0.05)
)
plot(curves$w, curves$likelihood, type = "l",
  xlab = "w", ylab = "standardised value")
lines(curves$w, curves$posterior, lty = 2)

}
\references{
Puza, B. and Bonfrer, A. (2018). A series of two-urn biased sampling
problems. \emph{Communications in Statistics - Theory and Methods}, 47(1),
80-91. \doi{10.1080/03610926.2017.1300282}
}
\seealso{
\code{\link[=inferW]{inferW()}} for point estimates and the test of \code{w = 1}.
}
