% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcmc.R
\name{MHALG}
\alias{MHALG}
\title{Metropolis-Hastings sampler for a single weight class}
\usage{
MHALG(
  B = 5,
  J = 20,
  N1vec = 150,
  N2vec = 200,
  N0vec = 50,
  M1vec = 40,
  M2vec = 60,
  mvec = 10,
  Cvec = 1,
  eta = 0.001,
  tau = 1000,
  mu0 = 0,
  sig0 = 0.001,
  muinit = 0,
  rinit = 0,
  del = 0.1,
  u = exp(20),
  v = 1,
  hf = 1
)
}
\arguments{
\item{B}{Integer; number of burn-in iterations.}

\item{J}{Integer; number of sampling iterations retained after burn-in.}

\item{N1vec, N2vec}{Numeric vectors; \code{N1} and \code{N2} for each observation.}

\item{N0vec}{Numeric vector; number of common items \code{N} for each
observation.}

\item{M1vec, M2vec}{Numeric vectors; sample sizes \code{m1} and \code{m2} for each
observation.}

\item{mvec}{Numeric vector; observed numbers of matches.}

\item{Cvec}{Integer vector; weight-class index for each observation.}

\item{eta, tau}{Numeric; shape and rate hyperparameters of the gamma prior
on the precision \code{lambda}.}

\item{mu0, sig0}{Numeric; mean and scale hyperparameters of the normal prior
on \code{mu}.}

\item{muinit, rinit}{Numeric; starting values for \code{mu} and the latent
weights.}

\item{del}{Numeric; random-walk proposal standard deviation(s).}

\item{u, v}{Numeric; parameters of the internal link function mapping the
latent scale to the positive weight \code{w}.}

\item{hf}{Integer; if \code{0}, use the log-scale form of the log full
conditional, otherwise the link-function form (the default).}
}
\value{
A list with components \code{muv} (sampled \code{mu}), \code{lamv} (sampled
\code{lambda}), \code{rm} (matrix of sampled latent weights, one column per class)
and \code{rar} (acceptance rate per class).
}
\description{
Draws from the posterior distribution of the bias parameter and its
hyperparameters for a set of two-urn matching observations that share a
common weight. This is the hierarchical extension of the inferential
problem of Puza and Bonfrer (2018), and is most useful when several
matching experiments are pooled.
}
\examples{
# Short illustrative run; use many more iterations for real inference.
set.seed(4172)
res <- MHALG(
  B = 5, J = 20,
  N1vec = 37, N2vec = 45, N0vec = 16,
  M1vec = 12, M2vec = 8, mvec = 3,
  Cvec = 1, del = 0.1
)
str(res)

}
\references{
Puza, B. and Bonfrer, A. (2018). A series of two-urn biased sampling
problems. \emph{Communications in Statistics - Theory and Methods}, 47(1),
80-91. \doi{10.1080/03610926.2017.1300282}
}
\seealso{
\code{\link[=restable]{restable()}} and \code{\link[=estprop]{estprop()}} to summarise and diagnose the output.
}
