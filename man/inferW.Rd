% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{inferW}
\alias{inferW}
\title{Inference on the bias parameter from an observed number of matches}
\usage{
inferW(
  m,
  N1,
  N2,
  N,
  m1,
  m2,
  prior = c("jeffreys", "flat"),
  wmax = 200,
  alternative = c("greater", "less")
)
}
\arguments{
\item{m}{Integer; number of matches for which the probability is required.}

\item{N1}{Integer; number of distinct items in urn 1.}

\item{N2}{Integer; number of distinct items in urn 2.}

\item{N}{Integer; number of items common to both urns.}

\item{m1}{Integer; sample size drawn from urn 1.}

\item{m2}{Integer; sample size drawn from urn 2.}

\item{prior}{Character; the prior on \code{w} used for the posterior mode.
\code{"jeffreys"} (the default) uses \code{f(w)} proportional to \code{1 / w}, as in the
paper; \code{"flat"} uses a uniform prior, so the posterior mode equals the
MLE.}

\item{wmax}{Numeric; upper bound of the search region for \code{w}. If the
likelihood is still increasing at \code{wmax}, the MLE is reported as
infinite.}

\item{alternative}{Character; the alternative hypothesis for the p-value.
\code{"greater"} (default) tests \code{w = 1} against \code{w > 1} (bias towards common
items); \code{"less"} tests against \code{w < 1}.}
}
\value{
A list with components:
\describe{
\item{mle}{Maximum likelihood estimate of \code{w} (possibly \code{Inf}).}
\item{posterior_mode}{Posterior mode of \code{w} under \code{prior}.}
\item{p_value}{P-value for testing \code{w = 1}, computed from the null
(\code{w = 1}) distribution of the number of matches.}
\item{alternative}{The alternative hypothesis used.}
\item{m}{The observed number of matches.}
}
}
\description{
"Inverts" the biased sampling model of \code{\link[=PROBM]{PROBM()}}: given the fixed urn
constants and a single observed number of matches \code{m}, it returns the
maximum likelihood estimate and posterior mode of the bias parameter \code{w},
and a p-value for testing no bias against a positive bias. This implements
the inferential problems (Problems 1-3) of Puza and Bonfrer (2018).
}
\details{
The likelihood is \code{L(w) = PROBM(m, ..., w)} and the posterior kernel under
the Jeffreys-type prior is \code{L(w) / w}. Both are maximised numerically on
\verb{(0, wmax]}. As noted in the paper, when the observed \code{m} is large enough
the likelihood is strictly increasing and the MLE is infinite; this is
detected and reported as \code{Inf}, while the posterior mode remains finite.
The p-value is the null-hypothesis (\code{w = 1}) tail probability of the number
of matches, obtained from \code{\link[=DISTM]{DISTM()}}.
}
\examples{
# Problem 3 of the paper (masked retailer promotion data)
inferW(m = 13, N1 = 292, N2 = 350, N = 192, m1 = 90, m2 = 59)

# Problem 2 of the paper (gender bias in committee selection, B = 2)
inferW(m = 2, N1 = 52, N2 = 52, N = 22, m1 = 9, m2 = 6)

}
\references{
Puza, B. and Bonfrer, A. (2018). A series of two-urn biased sampling
problems. \emph{Communications in Statistics - Theory and Methods}, 47(1),
80-91. \doi{10.1080/03610926.2017.1300282}
}
\seealso{
\code{\link[=likW]{likW()}} for the full likelihood and posterior curves.
}
