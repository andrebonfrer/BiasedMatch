% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/distribution.R
\name{PROBM}
\alias{PROBM}
\title{Probability mass for the number of matching items}
\usage{
PROBM(m = 3, N1 = 37, N2 = 45, N = 16, m1 = 12, m2 = 8, w = 2)
}
\arguments{
\item{m}{Integer; number of matches for which the probability is required.}

\item{N1}{Integer; number of distinct items in urn 1.}

\item{N2}{Integer; number of distinct items in urn 2.}

\item{N}{Integer; number of items common to both urns.}

\item{m1}{Integer; sample size drawn from urn 1.}

\item{m2}{Integer; sample size drawn from urn 2.}

\item{w}{Numeric (> 0); weight of a common item relative to a non-common
item. \code{w = 1} gives unbiased simple random sampling without replacement.}
}
\value{
A single numeric probability.
}
\description{
Computes the probability that exactly \code{m} items match across two samples
drawn without replacement from two overlapping urns, under the biased
selection scheme of Puza and Bonfrer (2018).
}
\details{
Urn 1 contains \code{N1} distinct items and urn 2 contains \code{N2} distinct items,
with \code{N} items common to both urns. A sample of size \code{m1} is drawn from
urn 1 and a sample of size \code{m2} from urn 2. Each item common to both urns
is given selection weight \code{w} while every other item has weight 1, so that
\code{w > 1} makes common items more likely to be selected. A match is an item
selected in both samples.

The implementation follows Theorem 2 of Puza and Bonfrer (2018). It sums
over \code{y1}, the number of common items drawn from urn 1, weighting the
conditional match probability given \code{y1} by the probability of \code{y1}. The
distribution of \code{y1} is Wallenius' univariate non-central hypergeometric
distribution (\code{\link[BiasedUrn:dWNCHypergeo]{BiasedUrn::dWNCHypergeo()}}); the conditional distribution of
matches given \code{y1} uses the bivariate Wallenius' distribution
(\code{\link[BiasedUrn:dMWNCHypergeo]{BiasedUrn::dMWNCHypergeo()}}). When \code{w = 1} the result reduces to the
hypergeometric mixture of Theorem 1.
}
\examples{
# Probability of exactly 3 matches (Example 1 of the paper, w = 2)
PROBM(m = 3, N1 = 37, N2 = 45, N = 16, m1 = 12, m2 = 8, w = 2)

}
\references{
Puza, B. and Bonfrer, A. (2018). A series of two-urn biased sampling
problems. \emph{Communications in Statistics - Theory and Methods}, 47(1),
80-91. \doi{10.1080/03610926.2017.1300282}
}
\seealso{
\code{\link[=DISTM]{DISTM()}} for the full distribution and \code{\link[=inferW]{inferW()}} for inference
on \code{w}.
}
