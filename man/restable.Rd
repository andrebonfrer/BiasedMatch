% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summaries.R
\name{restable}
\alias{restable}
\title{Summarise posterior draws from an MH sampler}
\usage{
restable(
  res,
  Bn = 2,
  Jn = length(res$muv) - Bn,
  rm.idx = 1:dim(res$rm)[2],
  sim = FALSE
)
}
\arguments{
\item{res}{List; output from one of the MH samplers.}

\item{Bn}{Integer; number of initial iterations to discard as burn-in.}

\item{Jn}{Integer; number of retained iterations to summarise.}

\item{rm.idx}{Integer vector; columns of \code{res$rm} (weight classes) to
summarise.}

\item{sim}{Logical; reserved for comparison against known parameters. Not
implemented and must be \code{FALSE}.}
}
\value{
A data frame with rows \code{Mean}, \code{Mode}, \code{Q025} and \code{Q975}, and
columns for \code{mu}, \code{lambda}, \code{sigma} and each weight class \code{r1}, \code{r2}, ...
}
\description{
Produces posterior means, modes and 95\% credible intervals for the
hyperparameters and weight classes returned by \code{\link[=MHALG]{MHALG()}}, \code{\link[=RMMHALG]{RMMHALG()}} or
\code{\link[=SMRMMHALG]{SMRMMHALG()}}.
}
\examples{
set.seed(5093)
res <- MHALG(
  B = 5, J = 40,
  N1vec = 37, N2vec = 45, N0vec = 16,
  M1vec = 12, M2vec = 8, mvec = 3, Cvec = 1, del = 0.1
)
restable(res, Bn = 5)

}
\seealso{
\code{\link[=MHALG]{MHALG()}}, \code{\link[=estprop]{estprop()}}.
}
